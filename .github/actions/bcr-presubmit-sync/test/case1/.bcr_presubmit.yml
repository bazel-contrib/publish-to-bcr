bcr_test_module:
  module_path: "examples"
  matrix:
    platform:
      - macos
      - ubuntu2004
    bazel: ["9.*", "8.x", "7.x"]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - //some/build:targets
        - -//but/not:this_one
      test_targets:
        - "//..."
